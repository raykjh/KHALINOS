# KHALINOS four-minute live demo script

Target: one continuous, unedited, 1× recording. Start from a prepared SixSense Outcome Preview; no Cloud execution has started before the recording.

## 0:00–0:12 — authorize the live run

Screen: authenticated KHALINOS Outcome Preview for the side-scroll destination profile.

Narration:

> KHALINOS turns one authorized goal into a bounded build and refuses completion without runtime proof. Only intake is pre-staged; execution has not started. I authorize it now.

Action: click **Authorize and execute** by 0:12.

## 0:12–0:50 — show the agentic execution UI

Screen: KHALINOS live execution.

Narration:

> This is a real Cloud Run Job, not a scripted progress animation. The selected profile determines the Capability Pack composition. The fixed agent ceiling stays at thirteen, while the active agent, bound Packs, Gemini calls, verifier state, execution ID, and M01 through M06 receipts come from Firestore state.

Point to the selected profile, Capability Packs, active agent, horse handoff, execution ID, Gemini calls, verifier, and milestone cargo as they change.

## 0:50–1:05 — architecture

Screen: `docs/architecture/khalinos-architecture.png` in Chrome.

Narration:

> The human owns authority. Gemini agents plan and judge through Google ADK. The trusted host binds least-authority Packs. Real Godot mechanics and rendering run before a role-separated verifier can pass the result. The verifier is separate inside KHALINOS, not an external audit organization.

## 1:05–1:20 — Google Cloud proof

Screen: Google Cloud Run Job execution for `khalinos-worker` in project `khalinos-agent-20260818`, region `asia-northeast3`.

Narration:

> Here is the live backend on Google Cloud: the fixed Worker Job, its execution, region, and running state. Vertex AI supplies Gemini 3.5 Flash and Nano Banana; Cloud Storage holds evidence, while Firestore holds the live projection.

## 1:20–2:55 — follow the live run

Screen: return to KHALINOS.

Narration cues, used only when the matching state appears:

> The Project Owner has issued dependency-safe Quests.

> The Visual roles are producing and comparing three renderable foundations.

> Deterministic Runtime is now exercising movement, automatic combat, destination victory, audio events, and animated effect frames in Godot.

> The Verifier can judge the bound criteria but cannot modify the artifact.

Leave silence when useful; keep the moving horse, active agent, calls, verifier, and receipts visible.

## 2:55–3:12 — PASS and receipt boundary

Screen: KHALINOS PASS state.

Narration:

> PASS means the mechanics, rendering, and criterion-bound verification all succeeded. The downloadable source is digest-bound to the receipt chain. A failure would stop safely instead of becoming a guessed completion.

Action: show **Download verified source** and the final receipt count. Do not open private tokens or raw identity details.

## 3:12–3:32 — generated side-scroll result

Screen: switch to the prepared side-scroll Godot editor, press **F6**, and let the generated scene begin from 0 m.

Narration:

> This is the generated side-scroll composition: the party advances right, attacks automatically, defeats enemies, and reaches a destination. Its visual presentation is intentionally prototype-level; the proof is executable behavior, not commercial polish.

## 3:32–3:50 — second composition

Screen: switch to the prepared Trinity start screen and click **START**.

Narration:

> The same fixed agent team also produced Trinity, but consumed a different Pack chain: top-down combat, profession progression, a license-receipted character atlas, and different effect-role bindings. This is constrained composition across two profiles, not a claim of universal game generation.

## 3:50–3:58 — close

Screen: KHALINOS PASS UI or architecture.

Narration:

> KHALINOS removes the supervision loop between one authorization and an evidence-backed result. If it is not proven, it is not complete.

Stop recording before 4:00.
